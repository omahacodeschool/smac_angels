<p>
<div class="row content-padding">
  <%- model_class = Template -%>
  <div class="page-header center-text content-small-padding">
    <h1>Templates</h1>
  </div>
  <table class="table table-striped">
    <tbody>
      <% @templates.each do |template| %>
        <tr>
          <td class="content-right-padding"><h1><%= link_to template.id, template_path(template) %> </h1></td>
          <td class="content-right-padding"><b><%= template.name %></b></td>
          <td class="content-right-padding"><%= template.content %></td>
          <td class="content-right-padding"><%=l template.created_at %></td>
          <td>
            <b>
              <%= link_to t('.edit', :default => t("helpers.links.edit")),
                          edit_template_path(template), :class => 'btn btn-mini' %>
              <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                          template_path(template),
                          :method => :delete,
                          :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                          :class => 'btn btn-mini btn-danger' %>
            </td>
          </b>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>

  <div class="row">
    <div class="large-12 small-12 content-bottom-padding center-text columns">
    <%= link_to t('.new', :default => t("helpers.links.new")),
                new_template_path,
                :class => 'btn button-link-blue btn-primary' %>
    </div>
  </div>            

</p>